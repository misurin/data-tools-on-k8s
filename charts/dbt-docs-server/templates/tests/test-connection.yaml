apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dbt-docs.fullname" . }}-test-connection"
  labels:
    {{- include "dbt-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dbt-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
